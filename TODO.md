# Package Install Commands

Use one of these commands to install the project packages.

## Install all packages from lock file
npm install


npm install multer

npm install bcryptjs jsonwebtoken

npm install nodemailer
